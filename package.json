{
  "name": "bfs-projects",
  "version": "1.0.0",
  "private": true,
  "description": "BFS Projects marketing site with an enquiry backend",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test server/*.test.js"
  },
  "dependencies": {
    "express": "5.2.1"
  }
}
